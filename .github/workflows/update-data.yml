name: Daily RSS Update

on:
  schedule:
    - cron: '0 6 * * *'  # Каждый день в 6:00 UTC (9:00 МСК)
  workflow_dispatch:      # Ручной запуск

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install xml2js
    
    - name: Run RSS parser
      run: node js/update-data.js
    
    - name: Check for changes
      id: verify-changed-files
      run: |
        if git diff --quiet data/events.json; then
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "ℹ️ Данные не изменились"
        else
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "✅ Найдены новые события!"
        fi
    
    - name: Commit and push changes
      if: steps.verify-changed-files.outputs.changed == 'true'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "RSS Bot"
        git add data/events.json
        git commit -m "🔄 Auto-update: $(date +'%Y-%m-%d %H:%M UTC')"
        git push
    
    - name: Summary
      run: |
        echo "## 📊 Результат обновления" >> $GITHUB_STEP_SUMMARY
        echo "- Время: $(date)" >> $GITHUB_STEP_SUMMARY
        echo "- Статус: ${{ steps.verify-changed-files.outputs.changed == 'true' && '✅ Обновлено' || '⏭️ Без изменений' }}" >> $GITHUB_STEP_SUMMARY
